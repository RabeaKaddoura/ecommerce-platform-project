apiVersion: v1
kind: Service
metadata:
  name: {{ include "payment-svc.fullname" . }}
  labels:
    {{- include "payment-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "payment-svc.selectorLabels" . | nindent 4 }}
