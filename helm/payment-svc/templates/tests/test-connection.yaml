apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "payment-svc.fullname" . }}-test-connection"
  labels:
    {{- include "payment-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "payment-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
