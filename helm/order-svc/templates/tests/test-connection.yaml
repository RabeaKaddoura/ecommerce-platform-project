apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "order-svc.fullname" . }}-test-connection"
  labels:
    {{- include "order-svc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "order-svc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
