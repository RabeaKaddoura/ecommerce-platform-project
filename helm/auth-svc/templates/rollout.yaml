apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: auth-rollout
  labels:
    app.kubernetes.io/name: auth-svc
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: auth-app
  strategy:
    canary:
      stableService: auth-stable
      canaryService: auth-canary
      trafficRouting:
        alb:
          ingress: alb-ingress
          servicePort: {{ .Values.port }}
      steps:
        - setWeight: 10
        - pause:
            duration: 2m
        - setWeight: 50
        - pause:
            duration: 2m
  template:
    metadata:
      labels:
        app: auth-app
    spec:
      serviceAccountName: {{ .Values.serviceAccountName }}
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: auth-db-secrets
      containers:
        - name: auth-container
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.port }}
          volumeMounts:
            - name: secrets-store
              mountPath: "/mnt/secrets"
              readOnly: true
          env:
            - name: DB_NAME
              value: {{ .Values.config.dbName | quote }}

            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: auth-db-secret
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: auth-db-secret
                  key: password
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: auth-db-secret
                  key: host
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: auth-db-secret
                  key: port
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: auth-app-secret
                  key: SECRET_KEY
                  
            - name: ALGORITHM
              value: {{ .Values.config.algorithm | quote }}
            - name: ACCESS_TOKEN_EXPIRE_MINUTES
              value: {{ .Values.config.accessTokenExpireMinutes | quote }}
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: {{ .Values.config.kafkaBootstrapServers | quote }}
          resources:
            requests:
              cpu: {{ .Values.resources.requests.cpu | quote }}
              memory: {{ .Values.resources.requests.memory | quote }}
            limits:
              cpu: {{ .Values.resources.limits.cpu | quote }}
              memory: {{ .Values.resources.limits.memory | quote }}